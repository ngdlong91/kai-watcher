version: '3.4'

services:
  kai-watcher:
    build:
      context: .
    image: kardiachain/kai-watcher
    container_name: "kai-watcher"
    command: "cmd"
    restart: always
    volumes:
      - .env:/go/bin/.env
